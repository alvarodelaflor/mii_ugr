version: '3'

services:
    nginx:
      build: ./nginx
      container_name: nginx_15408846
      ports:
        - '31010:80'
      depends_on:
      - nextcloud01
      - nextcloud02
    nextcloud01:
      image: 'nextcloud:21.0.0-apache'
      container_name: nextcloud01_15408846
      restart: always
      volumes:
      - nextcloud:/var/www/html
      environment:
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=password
      - MYSQL_HOST=mariadb
      - NEXTCLOUD_ADMIN_USER=admin
      - NEXTCLOUD_ADMIN_PASSWORD=password
      depends_on: 
      - mariadb
    nextcloud02:
      image: 'nextcloud:21.0.0-apache'
      container_name: nextcloud02_15408846
      restart: always
      volumes:
      - nextcloud:/var/www/html
      environment:
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=password
      - MYSQL_HOST=mariadb
      - NEXTCLOUD_ADMIN_USER=admin
      - NEXTCLOUD_ADMIN_PASSWORD=password
      depends_on: 
      - mariadb
    mariadb:
      image: "mariadb:10.4.8-bionic"
      container_name: mariadb_15408846
      command: "--transaction-isolation=READ-COMMITTED --binlog-format=ROW"
      restart: always
      volumes:
      - db:/var/lib/mysql
      environment:
      - MYSQL_ROOT_PASSWORD=admin
      - MYSQL_PASSWORD=password
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
    openldap:
      image: larrycai/openldap
      container_name: openldap_15408846
      ports:
        - '389:389'
        - '636:636'
      environment:
        - LDAP_ADMIN_USERNAME=admin
        - LDAP_ADMIN_PASSWORD=adminpassword
        - LDAP_USERS=user01,user02
        - LDAP_PASSWORDS=password1,password2

      volumes:
        - 'openldap_data:/bitnami/openldap'
volumes:
    openldap_data:
      driver: local
    nextcloud:
      driver: local 
    db:
      driver: local